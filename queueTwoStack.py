class MyQueue:
  def __init__(self):
	  self.A = []
	  self.B = []
	  self.enqueued = False

	def enqueue(self, item):
		if not self.enqueued
			while A != []:
				B.append(A.pop())
		A.append(item)
		self.enqueued = True

	def dequeue(self):
		if enqueued:
			A.append(B.pop())
		enqueued = False
		return A.pop()